<!DOCTYPE html>
<html>
<head>
	<title>New Category</title>
</head>
<body>
	<%= form_for [:admin, @category] do |f| %>
		<%= f.label :name, "Name" %>
		<%= f.text_field :name %>

		<%= f.submit %>
	<% end %>				
</body>
</html>